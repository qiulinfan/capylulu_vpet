# Formal V1 `running`: focused-listening alias

Do not generate independent artwork for this state.

The Codex V1 runtime requires six cells in atlas row 7 and uses them while a
task is active. Per the approved character design, each `running-NN.png` must
be an exact pixel copy of the corresponding `review-NN.png` focused-listening
frame. This keeps the runtime contract without retaining a generic running
action.
