# Formal V1 `running-left`: derived pointer drag

Do not generate this state independently.

`running-left` is the exact horizontal pixel mirror of the approved eight-frame
`running-right` mother sequence. Preserve frame order, cell placement, scale,
alpha, palette, facial construction, eye state, and runtime timing. A separate
generation is forbidden because it can change Lulu's face, size, gaze, or
motion rhythm between directions.

The runtime meaning is pointer/window drag toward screen-left. Mirroring makes
the head lead left while the body and limbs trail right.
